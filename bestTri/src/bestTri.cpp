#include <iostream>
#include <cstdlib>
#include <cstring>
#include <cmath>
#define N 100 // or your input
#define K 4
#define nCk 3921225
using namespace std;
int main(int argc, char const *argv[])
{
	/* code */
	int subsets[nCk][K]; 			// array to hold the indexes of the subsets
	bool dup_hit[N]
	int i,j,N_min,N_max,n,k;
	N_min = pow(n,k);
	N_max = pow(n,k+1);

	for(i = N_min; i < N_max; i++)
	{
		memset(dup_hit)
		while(i > 0)
		{

		}
	}

	return 0;
}
